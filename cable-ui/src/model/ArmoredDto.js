class ArmoredDto {
    constructor(key, label, nodes, isOpen) {
        this.key = key
        this.label = label
        this.nodes = nodes
        this.isOpen = isOpen
    }

}

module.exports = ArmoredDto;