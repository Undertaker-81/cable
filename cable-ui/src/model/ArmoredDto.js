class ArmoredDto {
    constructor(key, label, nodes, isOpen, hasNodes) {
        this.key = key
        this.label = label
        this.nodes = nodes
        this.isOpen = isOpen
        this.hasNodes = hasNodes
    }

}

module.exports = ArmoredDto;